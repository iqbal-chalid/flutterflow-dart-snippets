double getProgressValue(
  double targetValue,
  int currentStep,
  int totalStep,
) {
  /// MODIFY CODE ONLY BELOW THIS LINE

  return (targetValue * (currentStep / totalStep));

  /// MODIFY CODE ONLY ABOVE THIS LINE
}
